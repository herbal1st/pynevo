profiles:

  TEST:
    skin_profile: "DEV"
    profile_style: "DIRECT_VECTOR"
    move_speed: 0.5
    turn_speed: 540.0
    diameter_ratio: 1.0
    min_spawn_speed: 0.1
    companion_offset: [2.0, 5.0]

  DEFAULT:
    skin_profile: "DEV"
    profile_style: "DIRECT_VECTOR"
    move_speed: 4.0
    turn_speed: 540.0
    diameter_ratio: 6.0
    min_spawn_speed: 0.1
    companion_offset: [2.0, 5.0]

  TANK:
    skin_profile: "DEV"
    profile_style: "TANK"
    move_speed: 4.0
    turn_speed: 540.0
    diameter_ratio: 6.0
    min_spawn_speed: 0.1
    companion_offset: [2.0, 5.0]

  CAR:
    skin_profile: "DEV"
    profile_style: "CAR"
    move_speed: 4.0
    turn_speed: 540.0
    diameter_ratio: 6.0
    min_spawn_speed: 0.1
    companion_offset: [2.0, 5.0]


# ============================================================================
# PLAYER LIBRARY CONFIGURATION LEGEND
# ============================================================================
#
# skin_profile:     Name key referencing visual skin profile in skin.yaml.
# profile_style:    Steering mechanics selector ("DIRECT_VECTOR", "TANK", "CAR").
# move_speed:       Base translation velocity scale per frame (tiles/tick).
# turn_speed:       Maximum rotational velocity scale (degrees/sec).
# diameter_ratio:   Hit-box diameter scale relative to full tile width.
# min_spawn_speed:  Minimum terrain speed multiplier required for safe spawn.
# companion_offset: Follower target offset relative to player facing [X, Y] (tiles).
# ============================================================================